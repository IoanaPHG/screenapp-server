{
  "name": "screenapp",
  "version": "1.0.0",
  "description": "Screen sharing desktop app with WebRTC signaling",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "agent:publish": "C:\\Progra~1\\dotnet\\dotnet.exe publish .\\agent\\ScreenAppAgent\\ScreenAppAgent.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o .\\agent\\publish",
    "start": "node server.js",
    "desktop": "electron .",
    "dist": "npm run agent:publish && electron-builder --win nsis portable",
    "dist:portable": "npm run agent:publish && electron-builder --win portable",
    "dist:setup": "npm run agent:publish && electron-builder --win nsis",
    "rebuild": "electron-rebuild"
  },
  "dependencies": {
    "@nut-tree-fork/nut-js": "^4.2.6",
    "jsonwebtoken": "^9.0.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "electron": "^35.1.5",
    "electron-builder": "^24.13.3",
    "electron-rebuild": "^3.2.9"
  },
  "build": {
    "appId": "com.screenapp.desktop",
    "productName": "ScreenApp",
    "files": [
      "electron/**/*",
      "public/**/*",
      "server.js",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "agent/publish",
        "to": "agent/publish"
      }
    ],
    "win": {
      "signAndEditExecutable": false,
      "target": [
        "nsis",
        "portable"
      ]
    }
  }
}
